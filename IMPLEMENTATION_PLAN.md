# EduEval 落地实施方案（修订版 v2）

> 本文是 `DESIGN.md`（方案定稿）的**执行版**，基于已完成的数据调研（`docs/sources_inventory.md`）修订。
> v1 版按"从零构建知识库"估算；v2 依据实际获取的资料重新评估可行性，主要变化：
> ① 知识库由"Hy3 生成"改为"**K12-KGraph 导入 + 定向补充**"；
> ② 维度 3 超纲判断由"LLM 识别"改为"**确定性查表**"；
> ③ 任务按**是否依赖 Hy3 Key** 重排，把约 65% 工程量前移到无 Key 即可完成的阶段。

---

## 0. 阻塞项：Hy3 API Key 未配置 ⚠️

**现状**：全盘搜索确认，本机与项目中**均无 Hy3 端点与密钥**（无 `.env`、无环境变量）。

| 影响 | 说明 |
|---|---|
| 阻塞 | 样本生成、Judge 评分、公式/别名补充、学段识别 |
| **不阻塞** | 知识库导入与索引、年级映射、课标结构化、规则层、多格式解析、Judge 框架、缺陷注入器、Web 应用、检索测试集 |

**这是当前唯一的最高优先级阻塞项**，需你提供 `HY3_BASE_URL` / `HY3_API_KEY` / `HY3_MODEL`。
在此之前，按本文 Phase 0 推进（可完成约 65% 工程量），不空等。

---

## 1. 调研带来的四项可行性优化

### 优化 1：知识库由「生成」改为「导入 + 定向补充」

原计划 D2 需用 Hy3 生成 130–180 条知识条目。现已获得 **K12-KGraph**（人教版，初中 451 个 Concept，`definition` 填充率 100%）。

**改为**：导入 451 条作为基座 → 按 13 课题筛选 → 仅对**缺失字段**定向补充。工作量从「生成全部」降为「补充缝隙」。

### 优化 2：维度 3 超纲判断改为确定性查表（⭐ 最大收益）

**原方案**：由 Hy3 识别内容实际难度，再与声明年级比对 → 需 LLM 调用、有幻觉风险、难追溯。

**已验证可行的替代**：K12-KGraph 的 `appears_in` / `is_part_of` 边可把概念 100% 映射到年级册次：

```
初中 Concept 451 个 → 可映射年级册次：451（100.0%）
  七年级上册 80 | 七年级下册 91 | 八年级上册 78
  八年级下册 75 | 九年级上册 83 | 九年级下册 44
```

**改为**：
```
教学设计中抽取概念/术语 → K12-KGraph 检索命中概念 → 查表得所属年级册次
  → 与用户声明年级比对 → 超出声明范围 → 超纲信号（维度 3 硬证据）
```

**收益**：零 LLM 调用、零幻觉、**100% 可追溯**、确定性可复现。质量上也优于 LLM 判断。
同理，`prerequisites_for`（853 条边）为**维度 7 学情分析**提供前置知识的确定性依据。

### 优化 3：双层知识库，化解「公式稀缺」风险 ⭐

**调研发现的关键缺口**（初中 451 个 Concept 字段填充率）：

| 字段 | 填充率 | 影响 |
|---|---:|---|
| definition | **100%** | ✅ |
| importance | **100%** | ✅ 可映射课标认知层次（了解/理解/掌握/运用） |
| examples | 32.6% | 部分可用 |
| **formula** | **12.9%** | ❌ G0 知识准入要靠公式核验 |
| **aliases** | **12.2%** | ❌ 影响检索召回率 |
| pages | 0% | 改用 `appears_in` 推导位置 |

另：课标 OCR 文本中**公式被扭曲**（`(a+b)(a-b)=a²-b²` → 「（a+B）（a一B）=Q' B」），**公式不可从 OCR 取**。

**改为双层设计**：

| 层 | 内容 | 来源 | 用途 | 覆盖 |
|---|---|---|---|---|
| **Tier 1 概念索引** | 451 条初中概念（定义、年级、前置关系、相关概念） | K12-KGraph 直接导入 | 检索召回、**维度 3 超纲**、**维度 7 前置知识** | 100% |
| **Tier 2 可核验断言集** | 13 课题 × 8–15 条 = **130–180 条**核心断言（概念 + 公式 + 适用条件 + 常见错误 + 课标依据） | K12-KGraph 已有 formula（58 条）+ Hy3 生成 + **sympy 程序校验** + 人工抽检 | **G0 知识准入** | 13 课题全覆盖 |

**关键取舍**：Tier 2 不求覆盖全部 451 概念，只覆盖 13 课题的核心断言——与开发集课题范围一致，从而在可控工作量内达成"**评测集核心知识覆盖率 ≥ 90%**"的验收指标。范围外断言按设计返回 `NE`，属**可声明的边界**而非缺陷。

> 💡 **Leverage 你的领域优势**：你是 K-12 数学教师，Tier 2 的 130–180 条核心断言正是"高价值、低体量"的部分。建议流程：Hy3 生成草稿 → sympy 自动校验 → **你抽检关键条目**。这比让 LLM 从零生成更可靠，也更有说服力。

### 优化 4：任务按「是否依赖 Key」重排

将 12 天拆为**不依赖 Key 的 Phase 0** 与**依赖 Key 的 Phase 1**，避免空等：

- **Phase 0（立即可做，约 65% 工程量）**：知识库导入与索引、年级映射、课标结构化、规则层、解析层、Judge 框架（mock 可跑通）、缺陷注入器、Web 应用、检索测试集
- **Phase 1（Key 到位后）**：公式/别名补充、40 份样本生成、Judge 实评测、10 类验证实验

---

## 2. 修订后的实施路线

### Phase 0：无 Key 地基（约 6 天）

| # | 任务 | 产出 | 完成标志 |
|---|---|---|---|
| **P0-1** | 知识库 schema + 来源清单 | `src/kb/schema.py`<br>`src/kb/sources.yaml`<br>`docs/kb_scope.md` | schema 含 Tier1/Tier2 双类型；必填字段（来源/许可/版本）校验通过 |
| **P0-2** | K12-KGraph 导入 + FTS5 索引 | `src/kb/ingest.py`<br>`src/kb/retriever.py`<br>`data/kb/knowledge.jsonl`<br>`data/kb/knowledge.db` | 451 条初中概念入库；别名/定义查询可召回 Top-3；隔离区条目不参与检索 |
| **P0-3** | **概念 → 年级映射表**（优化 2） | `src/kb/grade_map.py`<br>`data/kb/concept_grade.json` | 451/451 概念可映射到年级册次；单条查询 < 10ms |
| **P0-4** | 课标文本结构化 | `src/kb/curriculum.py`<br>`data/kb/curriculum_junior.jsonl` | 从已 OCR 文本解析出「内容要求/学业要求/教学提示」条目，挂到四个领域 |
| **P0-5** | 规则层（确定性校验） | `src/eval/rules.py`<br>`src/eval/assertions.py` | sympy 公式/方程求解校验；**声明 vs 识别年级比对**；结构完整性检查 |
| **P0-6** | 多格式解析升级 | `src/parse/parsers.py`（PyMuPDF）<br>`src/parse/layout.py` | 5 格式输出页序/层级/备注/公式标记；`parse_status` 与置信度；空文本 PDF 返回 `ocr_required` |
| **P0-7** | Judge 拆分 + 编排 + 缓存 | `src/eval/judges/*`（5 类）<br>`src/eval/orchestrator.py`<br>`src/eval/cache.py` | mock 模式可跑通完整编排；缓存按「样本 hash+角色+提示版本+温度」命中 |
| **P0-8** | 聚合 + Web 可视化 | `src/eval/rubric.py`<br>`src/eval/aggregator.py`<br>`src/api/` + `web/` | 单样本端到端：上传 → 准入 → 8 维 → 可视化报告（维度卡片 + 证据引用 + 雷达图） |
| **P0-9** | 缺陷注入器 + 检索测试集 | `scripts/inject_defects.py`<br>`data/kb/retrieval_queries.jsonl` | 注入器记录「位置/类型/严重程度/期望准入状态/期望受影响维度」；检索查询 ≥100 条含期望命中 ID |

> **P0 里程碑 M1**：P0-3 + P0-5 完成后，一条含错误公式的样本应能被**规则层**判 `FAIL`（**无需 Key 即可验证**）。
> **P0 里程碑 M2**：P0-8 完成后，mock 模式可演示完整报告流程。

### Phase 1：依赖 Key（约 6 天）

| # | 任务 | 产出 | 完成标志 |
|---|---|---|---|
| **P1-1** | Tier 2 断言集构建 | `data/kb/assertions.jsonl` | 13 课题 × 8–15 条；formula 经 sympy 校验；未覆盖标记 `unverified` |
| **P1-2** | 公式/别名定向补充 | ingest 补充流程 | Tier 1 aliases 覆盖率提升（目标 ≥ 60%） |
| **P1-3** | 开发集生成（40 份） | `scripts/build_devset.py`<br>`data/samples/devset.jsonl` | 自然优质 10 / 一般 8 / 知识缺陷 10 / 教学缺陷 6 / 对抗 6；对抗占比 15% |
| **P1-4** | 参考样本采集 | `data/samples/reference/` | 10–15 份（**需你登录智慧教育平台下载**；仅存元数据 + 来源链接） |
| **P1-5** | 全量评测 | `scripts/run_eval.py` → `results/` | 40 份 × 10 维完整结果；Judge 调用约 750 次（缓存 + 并发 4 + 温度 0） |
| **P1-6** | 10 类验证实验 | `results/*.csv`<br>`results/metrics.json` | §4.4 十类验证有数据；失败案例可归因 |
| **P1-7** | 交付物 | `docs/datacard.md`<br>`docs/analysis_report.md`<br>README 更新 + demo | 数据卡、分析报告、运行说明齐备；红线检查（§5.4）全通过 |

---

## 3. 关键设计细节

### 3.1 Tier 1 / Tier 2 条目结构

**Tier 1（概念索引，K12-KGraph 导入）**
```json
{
  "tier": 1, "id": "math_7a_rjb_cpt1", "name": "正数",
  "definition": "大于0的数叫做正数…",
  "importance": "掌握", "grade": "七年级上册", "publisher": "人教版",
  "aliases": [], "prerequisites": ["math_7a_rjb_cpt2"], "related": ["..."],
  "source": "K12-KGraph", "license": "CC BY-NC-SA 4.0", "std_ref": null
}
```

**Tier 2（可核验断言，G0 准入专用）**
```json
{
  "tier": 2, "id": "as-0701-003", "topic": "一元一次方程", "grade": "七年级",
  "assertion": "一元一次方程标准形式 ax+b=0 (a≠0)",
  "formula": "a*x + b = 0", "conditions": "a ≠ 0；未知数次数为 1",
  "common_errors": ["忽略 a≠0", "移项未变号"],
  "verification": { "method": "sympy_solve", "status": "verified", "checked_at": "..." },
  "std_ref": "课标·第四学段【内容要求】1.方程与不等式",
  "source": "...", "license": "..."
}
```
> `verification.status` 仅 `verified` 的条目参与 G0 判定；`unverified` 一律返回 `NE`。

### 3.2 维度 3 超纲判断（确定性规则）

```
声明年级（用户填写）  →  允许年级集合（如"七年级" = {七上, 七下}）
抽取概念 → K12-KGraph 检索 → concept_grade.json 查表 → 实际年级集合
判据：
  - 实际 ⊆ 允许            → 5 分档（完全匹配）
  - 越界集合非空但占比小    → 3–4 分档（少量超纲）
  - 越界概念属核心概念      → 1–2 分档（严重超纲）
  - 缺少年级/版本/课题元数据 → NE（强制规则，不由 Judge 猜测）
```

### 3.3 Judge 调用预算（修订）

| 实验 | 样本 | Judge 调用 |
|---|---:|---:|
| 主评测 | 40 | ~240 |
| 测试—重测 | 40 | ~240 |
| 缺陷梯度 | 15 | ~90 |
| 跨格式等价集 | 5×4 | ~120 |
| 对抗性 | 6 | ~36 |
| **合计** | | **~750（含重试约 900）** |

> 因维度 3 改为确定性规则，Judge 调用量较 v1 略降；并发上限 4、`temperature=0`、磁盘缓存。

---

## 4. 风险与降级方案

| 风险 | 等级 | 对策 |
|---|---|---|
| **Hy3 Key 未到位** | 🔴 最高 | Phase 0 先行（65% 工程量）；Key 到位后直接进 Phase 1 |
| **Tier 2 公式覆盖率不足** | 🔴 高 | 优先覆盖 13 课题核心断言（非全部概念）；sympy 自动校验；你抽检关键条目；未覆盖按 `NE` 如实报告 |
| **K12-KGraph 的 NC 许可** | 🟡 中 | 已记录：须署名 + 相同方式共享 + 不重发原始文件；数据卡标注 NC 限制。若评委关注，可改为**仅用其做内部构建、仓库只发自建 Tier-2 断言** |
| **12 天工期偏紧** | 🟡 中 | 保底线见下；Phase 0 与你的时间可并行（不依赖 Key） |
| **参考样本需人工下载** | 🟡 中 | 若无法获取，参考样本验证（§4.4 第 6 项）改为"仅报告通过率与案例分析"，并在报告中说明该限制 |
| **课标 OCR 公式失真** | 🟢 低 | 已规避：公式一律不从 OCR 取，只用其语义内容 |

**保底线（时间不足时必交付）**：
1. 双层知识库（Tier 1 全量 + Tier 2 至少 8 课题 / 100 条）
2. G0 知识准入 PASS/FAIL/NE + **维度 3 确定性超纲判断**
3. 8 维加权评分 + 红线 + NE 阻断
4. 3 种核心格式（MD/TXT/DOCX）
5. 40 份开发集 + 缺陷注入
6. 验证实验：知识覆盖检索、准入判别、教学缺陷判别、稳定性
7. README + 数据卡 + 结果表

**可延后**：PDF/PPTX 完整支持、运行时 OCR、跨格式等价集、对抗性全套、参考样本、demo 精修。

---

## 5. 立即开始的下一步

**需要你先做两件事**：

1. **提供 Hy3 接入信息**（`HY3_BASE_URL` / `HY3_API_KEY` / `HY3_MODEL`）→ 解锁 Phase 1。
2. **确认 Tier 2 断言集的课题优先级**——若时间紧，13 课题是否可先做 8 个？

**我可立即开始（无需等 Key）**：Phase 0 的 P0-1 → P0-3，产出：
- `src/kb/schema.py`（双层条目结构与必填校验）
- `src/kb/sources.yaml`（来源与许可清单，含 K12-KGraph 的 NC 约束）
- `docs/kb_scope.md`（13 课题闭环清单与覆盖声明）
- `src/kb/ingest.py` + `grade_map.py`（导入 451 条 + 生成年级映射表）

要我现在就开工 P0-1 → P0-3 吗？
